import React from 'react';
import {useStyles} from '../../../../assets/utils/utils.js'
const MathForCom = () => {
    const classes = useStyles();
    return (
        <div className={classes.root}>
          mc  comming soon
        </div>
    );
};

export default MathForCom;